Milestones
==========
X render two characters
X translation
X rotation
X scaling
X changing colors
X selection ( using slicing - make visible )
  shapes get truncated
X scaling - rel. to lower-left corner of prim.
X rotating - rel. to center of prim.
  show bounding box, and allow dragging the margins
  when rotating, show rotating center
  rotating center can be moved ( keys / mouse )
  mouse wheel scales rel. to centre
  anti-aliasing
  double-buffering
X mouse selection
X tab selection
  actions ( undo ) 
  mouse over - highlight hovered objects

After all this basic functionality is implemented, try using waf as
a build system. ( note: needs python )

isolate current issue: rotation / clipping
